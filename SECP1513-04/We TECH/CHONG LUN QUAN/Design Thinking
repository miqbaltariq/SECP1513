# Assuming you have a GitHub repository cloned locally
# Change to the directory of your repository
cd path/to/your/repo

# Create a new PDF file or use an existing one
# Copy the PDF file into your repository directory

# Stage the file
git add yourfile.pdf

# Commit the changes
git commit -m "Add PDF file"

# Push the changes to GitHub
git push origin main
